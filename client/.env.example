# Optional private Chinese-study mode.
# Comma-separated usernames who should see the private /chinese workspace.
# If left blank, the frontend falls back to admin-only access.
VITE_CHINESE_MODE_USERS=
